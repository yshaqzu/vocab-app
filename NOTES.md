# NOTES

앞으로 해보고 싶은 기능 아이디어 목록입니다.

- [ ] 복습 방식을 여러 개 만들어서 사용자가 선택할 수 있게 하기
  - [x] 라운드 기반 방식 (기본, 현재 구현됨)
  - [x] SM-2 스타일 간격 반복 방식
  - [ ] 몰랐던 카드가 큐 중간에 바로 랜덤하게 다시 끼어드는 방식
- [x] 단어장 목록(`/decks`) 페이지를 "둘러보기"와 "관리"로 분리하기
  - `/decks`는 둘러보기, `/decks/manage`는 단어장 추가/이름수정/삭제 전용으로 분리 완료
- [x] 후리가나(읽는 법)를 카드 앞면에 바로 보여주지 말고, "몰랐어요/맞혔어요" 전에
      뜻/예문 확인할 때(뒤집었을 때) 같이 나오게 바꾸기
  - `Flashcard.tsx`에서 뒤집었을 때만 단어 위에 `<ruby>/<rt>`로 후리가나 표시하도록 수정 완료

## 2026-09-22 진행 상황

- [x] 사진으로 카드 자동 추가(OCR) 기능 완성 — Gemini 3.8 Flash + 3.6 Flash 폴백,
      실제 사진 테스트까지 완료 (중간에 Vertex AI API 미활성화로 503 나던 것도 해결)
- [x] 단어장/카드 수정·삭제 기능 완성 (`/decks/[deckId]/manage` 신설)
- [ ] 다음: 위 "단어장 목록 분리" 먼저 해볼지, SM-2 간격 반복으로 갈지 정하기

## 2026-09-23 진행 상황

- [x] 후리가나 뒤집었을 때만 표시 (단어 위에 `<ruby>/<rt>`로 수정)
- [x] 단어장 목록 `/decks`(둘러보기) / `/decks/manage`(관리) 분리
- [x] 카드/단어장 추가 UI를 학습 화면에서 완전히 분리 — 전용 페이지
      (`/decks/manage/new`, `/decks/[deckId]/manage/new`)로 이동, "+" 버튼으로 진입
- [x] 단어장 추가 후 바로 `/decks`로 이동
- [x] 학습 시작 시 / "처음부터 다시 복습하기" 시 카드 순서 랜덤 셔플
- [x] SM-2 간격 반복 학습 모드 완성
  - `/decks/[deckId]`가 라운드/SM-2 모드 선택 화면이 되고, 실제 라운드 로직은
    `/decks/[deckId]/round`로 이동, SM-2는 `/decks/[deckId]/srs`로 신설
  - `POST /api/cards/[cardId]/review`로 서버에서 2단계(맞혔어요/몰랐어요) 평가 기반
    SM-2 계산 (repetitions/interval/easeFactor/dueDate)
  - "SM-2 진행 초기화" 버튼(`POST /api/decks/[deckId]/srs-reset`)으로 덱 전체 SM-2
    진행 상황을 언제든 리셋 가능
  - 콘솔에서 같은 카드 연속 두 번 채점해서 `repetitions=2`, `interval=6`으로
    정확히 넘어가는 것까지 직접 확인 완료
- [ ] 다음: 몰랐던 카드가 큐 중간에 랜덤하게 다시 끼어드는 방식 검토
