# NOTES

앞으로 해보고 싶은 기능 아이디어 목록입니다.

- [x] 복습 방식을 여러 개 만들어서 사용자가 선택할 수 있게 하기
  - [x] 라운드 기반 방식 (기본)
  - [x] SM-2 스타일 간격 반복 방식
  - [x] 몰랐던 카드가 큐 중간에 바로 랜덤하게 다시 끼어드는 방식 (즉시 재등장 모드)
- [x] 단어장 목록(`/decks`) 페이지를 "둘러보기"와 "관리"로 분리하기
- [x] 후리가나(읽는 법)를 카드 앞면에 바로 보여주지 말고, 뒤집었을 때 같이 나오게 바꾸기

## 2026-09-22 진행 상황

- [x] 사진으로 카드 자동 추가(OCR) 기능 완성 — Gemini 3.8 Flash + 3.6 Flash 폴백,
      실제 사진 테스트까지 완료 (중간에 Vertex AI API 미활성화로 503 나던 것도 해결)
- [x] 단어장/카드 수정·삭제 기능 완성 (`/decks/[deckId]/manage` 신설)

## 2026-09-23 진행 상황

- [x] 후리가나를 카드 뒤집었을 때만 보이게 변경 (ruby로 단어 위에 표시)
- [x] 단어장 목록을 둘러보기(`/decks`)/관리(`/decks/manage`)로 분리
- [x] 단어장/카드 추가를 별도 페이지(`/decks/manage/new`, `/decks/[deckId]/manage/new`)로
      분리하고, "+" 버튼으로 진입하게 변경
- [x] 학습 시작/재시작 시 카드 순서 랜덤 셔플
- [x] SM-2 간격 반복 학습 모드 완성
  - `/decks/[deckId]`가 라운드/SM-2/즉시 재등장 모드 선택 화면이 되고, 라운드 로직은
    `/decks/[deckId]/round`로 이동, SM-2는 `/decks/[deckId]/srs`로 신설
  - `POST /api/cards/[cardId]/review`로 서버에서 2단계(맞혔어요/몰랐어요) 평가 기반
    SM-2 계산 (repetitions/interval/easeFactor/dueDate)
  - "SM-2 진행 초기화" 버튼(`POST /api/decks/[deckId]/srs-reset`)으로 덱 전체 SM-2
    진행 상황을 언제든 리셋 가능
  - 콘솔에서 같은 카드 연속 두 번 채점해서 repetitions=2, interval=6으로 정확히
    넘어가는 것까지 직접 확인 완료 (커밋 `737bd62`)

## 2026-09-24 진행 상황

- [x] 즉시 재등장 모드 완성 (`/decks/[deckId]/requeue`)
  - "몰랐어요"를 누른 카드를 이번 라운드가 끝날 때까지 기다리지 않고, 화면에서
    정하는 "최소~최대 장 뒤" 범위(기본 2~4장) 안 랜덤한 위치에 바로 다시 끼워넣음
  - 범위값은 DB에 저장하지 않고 화면을 열 때마다 기본값으로 리셋 (설정 페이지는
    나중에 별도로 만들 예정)
  - 큐 길이보다 범위가 크면 범위를 줄여서 맨 뒤로 보내는 것까지 처리
  - 10,000회 반복 테스트로 재등장 위치 분포, 짧은 큐/과한 범위 예외 케이스까지
    확인 완료 (커밋 `8fb973d`)
- [ ] 다음: NOTES.md 상단의 복습 방식 아이디어 3개가 모두 완성됨 — 새 기능
      아이디어가 생기면 여기에 추가하기
