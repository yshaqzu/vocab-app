# "즉시 재등장" 학습 모드 설계

생디와 Cowork 세션에서 같이 정한 설계입니다. 로컬 Claude Code에게 이 파일을 보여주고
"이 설계대로 구현해줘"라고 하면 됩니다.

## 1. 배경

지금 라운드 모드는 "몰랐어요"를 누른 카드를 `missedThisRound`에 모아뒀다가, 이번
라운드 큐가 다 끝난 뒤에야 그 카드들로 새 라운드를 시작한다. 이번에 추가하는 모드는
그렇게 기다리지 않고, "몰랐어요"를 누른 카드를 **같은 큐 안에서 몇 장 뒤에 랜덤한
위치로 바로 다시 끼워넣는다** (Anki의 "again" 카드가 세션 안에서 금방 다시 나오는
느낌).

## 2. 확정된 결정

- 기존 라운드 모드(`/decks/[deckId]/round`)와 SM-2 모드(`/decks/[deckId]/srs`)는
  건드리지 않는다. 이건 **완전히 새로운 세 번째 모드**로 추가한다.
- "몇 장 뒤에 다시 나올지"의 범위(최소~최대)는 **그때그때 화면에서** 숫자 입력으로
  정한다. DB에 저장하지 않고, 화면을 새로 열면 기본값(최소 2, 최대 4)으로 리셋되는
  단순한 방식으로 한다. (나중에 설정 페이지가 생기면 그쪽으로 옮길 수도 있음 — 지금은
  거기까지 안 만든다.)
- DB에는 새 필드가 필요 없다. 기존 `mastered` 필드를 라운드 모드와 똑같은 의미로
  그대로 쓴다 (한 세션 안에서 "맞혔어요"를 받으면 mastered: true).

## 3. 큐 동작

- 처음 화면을 열면, 이 덱의 안 외운(mastered가 false인) 카드들을 무작위로 섞어서
  큐를 만든다 (라운드 모드 초기 로직과 동일).
- **"맞혔어요"**: 라운드 모드와 동일하게 서버에 `mastered: true`로 저장하고, 큐
  맨 앞(지금 카드)을 제거한다.
- **"몰랐어요"**: 서버는 건드리지 않는다(mastered는 그대로 false). 큐 맨 앞(지금
  카드)을 제거한 뒤, 남은 큐 중에서 "최소~최대" 입력값 사이의 랜덤한 위치를 골라
  그 자리에 이 카드를 다시 끼워넣는다.
  - 예: 최소 2, 최대 4면, 지금 카드를 뺀 나머지 큐에서 2번째~4번째 자리 사이
    무작위 위치에 다시 넣는다 (바로 다음 카드로는 절대 안 나옴).
  - 남은 큐 길이가 범위보다 짧으면(예: 큐에 카드가 1장밖에 안 남았으면), 범위를
    큐 길이에 맞춰 줄여서(clamp) 맨 뒤에 넣는다.
- 큐가 완전히 비면(모든 카드가 최소 한 번은 "맞혔어요"를 받았으면) 완료 화면을
  보여준다. 라운드 모드처럼 별도의 "이번 라운드에서 틀린 카드" 화면은 필요 없다 —
  이 모드는 "몰랐어요" 카드를 큐 밖으로 내보내지 않고 계속 큐 안에서 돌리기
  때문이다.

## 4. 화면 구성

### `/decks/[deckId]` (모드 선택 화면, 수정)

- 기존 "라운드 모드로 학습" / "SM-2 모드로 학습" 버튼 아래에
  "즉시 재등장 모드로 학습" 버튼을 추가해서 `/decks/[deckId]/requeue`로 연결한다.

### `/decks/[deckId]/requeue` (신규)

- `/decks/[deckId]/round`를 기반으로 만들되, 아래처럼 다르게 한다.
  - 화면 위쪽(덱 이름 근처)에 숫자 입력 두 개를 둔다: "최소 [ ] ~ 최대 [ ] 장 뒤에
    다시 등장". 기본값은 최소 2, 최대 4. 학습 중에도 언제든 값을 바꿀 수 있게 한다
    (바꾼 값은 그 다음 "몰랐어요"부터 적용).
  - 최소값이 최대값보다 커지지 않도록 입력을 막거나(예: 최대값을 최소값 이상으로
    자동 보정), 최소 0 이상만 허용한다.
  - "몰랐어요" 로직을 위 3번 섹션의 랜덤 재삽입 방식으로 구현한다 (라운드 모드의
    `missedThisRound` 방식은 쓰지 않는다).
  - 큐가 비었을 때: "다 외웠어요!" + "처음부터 다시 복습하기" 버튼을 보여준다
    (라운드 모드의 `handleResetReview`와 동일하게 `POST /api/decks/[deckId]/reset`
    호출 후 큐를 새로 무작위로 만든다).
  - "← 학습 모드 선택"(`/decks/[deckId]`), "카드 관리"(`/decks/[deckId]/manage`)
    링크도 다른 학습 화면들과 동일하게 상단에 둔다.
  - 카드 표시는 기존 `Flashcard` 컴포넌트를 그대로 재사용한다.

## 5. 확인할 점

- 라운드 모드(`/decks/[deckId]/round`)와 SM-2 모드(`/decks/[deckId]/srs`)는 기존
  동작을 그대로 유지해야 한다 (이 작업으로 건드리지 않기).
- "몰랐어요" 누른 카드가 실제로 바로 다음 카드로는 안 나오고, 설정한 범위 안의
  랜덤한 위치에서 다시 나오는지 확인한다.
- 최소/최대 범위를 큐 길이보다 크게 넣어도 에러 없이 동작하는지(맨 뒤에 들어가는지)
  확인한다.
- tsc/eslint/next build 통과 확인.
