# SM-2 학습 진행 초기화 기능 설계

생디와 Cowork 세션에서 같이 정한 설계입니다. 로컬 Claude Code에게 이 파일을 보여주고
"이 설계대로 구현해줘"라고 하면 됩니다.

## 1. 배경

SM-2 모드는 한 번 채점한 카드를 최소 내일 이후로 미루기 때문에, 테스트하거나 다시
처음부터 연습하고 싶을 때 오늘 복습할 카드가 금방 없어진다. 이걸 언제든 리셋할 수 있는
버튼이 필요하다.

## 2. 확정된 결정

- **범위**: 개별 카드가 아니라 **덱 전체**의 SM-2 진행 상태를 한 번에 초기화한다.
- **위치**: `/decks/[deckId]/srs` (SM-2 학습 화면) 상단에 **항상** 보이는 버튼으로 둔다
  (복습할 카드가 남아있어도, 없어도 항상 누를 수 있음).
- 라운드 모드(`mastered` 등)에는 영향을 주지 않는다. SM-2 전용 필드만 초기화한다.

## 3. 동작

"SM-2 진행 초기화" 버튼을 누르면, 이 덱에 속한 모든 카드에 대해 아래처럼 되돌린다
(새 카드를 만들 때의 기본값과 동일):

```
srsRepetitions = 0
srsInterval    = 0
srsEaseFactor  = 2.5
srsDueDate     = 지금 (바로 복습 대상)
```

확인 없이 바로 실행하지 말고, `window.confirm("이 단어장의 SM-2 진행 상황을 모두 초기화할까요?")`
같은 확인 절차를 한 번 거친다 (되돌릴 수 없는 동작이므로).

초기화가 끝나면 화면의 카드 목록을 새로고침해서(다시 `GET /api/cards?deckId=` 호출),
초기화된 카드들이 바로 "오늘 복습할 카드"로 나타나게 한다.

## 4. API

### `src/app/api/decks/[deckId]/srs-reset/route.ts` (신규)

- `POST`, body 없음.
- 해당 `deckId`에 속한 모든 카드를 `prisma.card.updateMany`로 찾아서 위 4개 필드를
  기본값으로 되돌린다.
- 성공하면 몇 개의 카드가 초기화됐는지(`count`) 정도만 반환하면 충분하다.

## 5. 화면 변경

### `src/app/decks/[deckId]/srs/page.tsx` (수정)

- 상단(덱 이름 근처, "← 학습 모드 선택" / "카드 관리" 링크 옆이나 아래)에
  "SM-2 진행 초기화" 버튼을 추가한다. 로딩 상태와 무관하게 항상 보이게 한다.
- 클릭 시:
  1. `confirm()`으로 한 번 확인한다.
  2. `POST /api/decks/${deckId}/srs-reset` 호출.
  3. 성공하면 `GET /api/cards?deckId=` 를 다시 불러와서 `dueCards`/`currentIndex`를
     처음 로드했을 때와 같은 방식으로 다시 계산한다 (지금 있는 초기 로드 로직을
     함수로 빼서 재사용하면 편하다).
  4. 실패하면 콘솔에 에러를 남기고 사용자에게 간단히 알려준다(`alert` 등).

## 6. 확인할 점

- 라운드 모드 관련 필드(`mastered` 등)는 절대 건드리지 않는다.
- 초기화 버튼은 다른 덱의 카드에는 영향을 주지 않는다 (`deckId`로 필터링).
- tsc/eslint/next build 통과 확인.
