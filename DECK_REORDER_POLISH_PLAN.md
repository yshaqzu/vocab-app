# 단어장 드래그 순서 변경 다듬기

지난번 `DECK_REORDER_PLAN.md`로 만든 드래그 순서 변경 기능을 실제로 써보고
정한 다듬기 작업입니다. 로컬 Claude Code에게 이 파일을 보여주고
"이 설계대로 구현해줘"라고 하면 됩니다.

## 1. 확정된 결정

- 손잡이(`⠿`) 위치를 지금 이름 왼쪽에서 **오른쪽 끝**(카드 개수/수정/삭제
  버튼보다도 더 오른쪽, 한 줄의 맨 끝)으로 옮긴다.
- 드래그로 순서가 바뀔 때, 항목들이 스냅되듯 바로 자리를 바꾸지 않고
  **부드럽게 밀려나는 애니메이션**을 넣는다. 작은 애니메이션 라이브러리
  `motion`(옛 이름 framer-motion, React 공식 후속 패키지)을 새로 설치해서 쓴다.

```bash
npm install motion
```

## 2. `src/app/decks/manage/page.tsx` 변경

### 손잡이 위치

지금은 `<li>` 안 왼쪽에 `<span>⠿</span>`이 이름 앞에 있는데, 이걸 오른쪽 끝
(지금 "삭제" 버튼보다 더 뒤)으로 옮긴다. 나머지 레이아웃(이름, 카드 개수,
수정/삭제 버튼)은 그대로 두고 손잡이만 맨 끝에 하나 더 붙이면 된다.

### 부드러운 순서 변경 애니메이션

- `motion/react`에서 `motion`을 import한다: `import { motion } from "motion/react";`
- 드래그 가능한(이름 수정 중이 아닌) `<li>`를 `<motion.li>`로 바꾸고
  `layout` prop을 추가한다. `key={deck.id}`는 지금처럼 그대로 유지한다
  (motion이 layout 애니메이션을 하려면 각 항목이 리스트 안에서 자기 정체성을
  유지해야 하므로 key가 중요하다).
  - `draggable`, `onDragStart`, `onDragOver`, `onDrop`, `onDragEnd`,
    `className` 등 지금 있는 속성/이벤트는 `motion.li`에 그대로 옮긴다
    (HTML 드래그 앤 드롭 동작 자체는 지금처럼 유지, 애니메이션만 추가되는
    것).
  - `transition={{ duration: 0.2 }}` 정도로 살짝 빠르게 움직이게 한다
    (너무 느리면 답답해 보임).
- 이름 수정 모드인 `<li>`(입력칸으로 바뀌는 쪽)는 그대로 일반 `<li>`로 둬도
  된다(그 항목은 드래그 불가라서 애니메이션이 크게 중요하지 않음). 다만
  화면 전체가 어색해 보이면 이쪽도 `motion.li`로 통일해도 괜찮다.

## 3. 확인할 점

- 손잡이가 오른쪽 끝에 잘 붙어 있고, 클릭해도 실수로 수정/삭제가 눌리지
  않는지 확인한다.
- 단어장을 드래그해서 다른 위치에 놓았을 때, 사이에 있던 항목들이 스냅되지
  않고 부드럽게 밀려나는 게 보이는지 확인한다.
- 기존 드래그 동작(놓으면 서버에 저장, 실패 시 되돌리기, 이름 수정 모드에서
  드래그 안 되는 것)은 그대로 유지되는지 확인한다.
- tsc/eslint/next build 통과 확인.
