# 단어장 드래그: 놓기 전에 실시간으로 자리 바뀌게 하기

지금은 마우스를 "놓아야"(`onDrop`) 순서가 바뀌는데, 생디가 원하는 건 마우스를
누른 채로 다른 항목 위를 지나가는 동안 **바로바로** 자리가 바뀌어 보이는
것(트렐로/노션 같은 느낌)입니다. 로컬 Claude Code에게 이 파일을 보여주고
"이 설계대로 구현해줘"라고 하면 됩니다.

## 1. 지금과 달라지는 부분

- 지금: `onDragOver`는 `preventDefault()`만 하고, 실제 순서 변경(`decks` 배열
  재배치)은 `onDrop`에서만 일어난다.
- 바꿀 것: 드래그 중인 항목이 다른 항목 위로 들어올 때(`onDragEnter`) 그
  즉시 `decks` 배열을 재배치해서 화면에 바로 반영한다. 마우스를 누르고
  있는 동안 계속 자리가 바뀌는 걸 보여주고, 놓는 순간에는 이미 정해진
  순서를 서버에 저장하기만 하면 된다.

## 2. `src/app/decks/manage/page.tsx` 변경

### 드래그 시작할 때 원래 순서를 기억해두기

- 드래그가 실패하거나 서버 저장이 실패했을 때 되돌릴 수 있도록, `onDragStart`
  시점의 `decks` 순서를 어딘가에 저장해둔다(`useRef`로 `preDragDecksRef` 같은
  이름을 만들어서, 리렌더링과 상관없이 값만 기억하게 하면 된다).

### 드래그 중 실시간 재배치: `onDragEnter` 추가

- 각 항목의 (드래그 이벤트가 붙어있는) `div`에 `onDragEnter` 핸들러를
  추가한다.
- 지금 드래그 중인 항목(`draggingId`)이 자기 자신이 아닌 다른 항목 위로
  들어오면, 그 즉시 `decks` 배열에서 드래그 중인 항목을 지금 위치로
  옮긴 새 배열을 만들어 `setDecks`로 반영한다 (지금 `handleDropDeck`에
  있는 "배열에서 빼서 다시 끼워넣기" 로직을 재사용하되, 서버에 저장하는
  부분은 여기서 하지 않는다 — 화면 반영만).
- `onDragOver`는 지금처럼 `preventDefault()`만 유지한다(드롭을 허용하기
  위해 필요).

### 서버 저장은 `onDragEnd`에서

- `onDrop`은 `preventDefault()`만 하고 특별히 저장하지 않아도 된다(실시간
  재배치가 이미 다 끝난 상태이므로).
- `onDragEnd`(드래그가 끝나는 시점 — 놓았을 때든, 바깥에 놓아서 취소됐을
  때든 항상 호출됨)에서:
  1. `setDraggingId(null)`
  2. 지금 `decks` 순서(이미 실시간으로 반영된 순서)를
     `POST /api/decks/reorder`로 서버에 저장한다.
  3. 실패하면 `preDragDecksRef`에 저장해둔 원래 순서로 `setDecks`를
     되돌리고 에러 메시지를 보여준다.

### 정리

- `handleDropDeck` 함수는 이제 "재배치 + 저장"을 한 번에 하던 걸, "재배치만
  하는 부분"(`onDragEnter`용)과 "저장만 하는 부분"(`onDragEnd`용)으로
  나누는 게 자연스럽다. 함수를 두 개로 쪼개거나, 하나를 다른 하나 안에서
  호출하는 식으로 정리해도 된다 — 동작만 위 설명대로 맞으면 구조는
  로컬 Claude Code가 편한 대로 짜도 된다.

## 3. 확인할 점

- 단어장을 누른 채로 다른 단어장들 위를 천천히 지나가면, 마우스를 놓기
  전인데도 항목들이 바로바로 자리를 바꾸며 부드럽게 밀려나는지 확인한다.
- 마우스를 놓으면 그 순간의 화면 순서 그대로 서버에 저장되고, 새로고침해도
  유지되는지 확인한다.
- 저장이 실패하는 상황(예: 네트워크 끊김 흉내)에서 원래 순서로 되돌아가고
  에러 메시지가 뜨는지 확인한다.
- 이름 수정 모드, 손잡이 위치, 수정/삭제 버튼 클릭 등 지난 작업 내용은
  그대로 유지되는지 확인한다.
- tsc/eslint/next build 통과 확인.
